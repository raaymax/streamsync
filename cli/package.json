{
	"name": "streamsync-cli",
	"version": "0.0.0",
	"scripts": {
		"test": "pytest tests -o log_cli=true ",
		"lint": "mypy ./src/streamsync --exclude app_templates/*"
	},
	"dependencies": {
		"streamsync-ui": "*"
	}
}
